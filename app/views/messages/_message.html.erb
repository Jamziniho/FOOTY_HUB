 <div id="message-<%= message.id %>">
  <small>
  <strong class="msgname"><%= message.user.first_name %></strong>
  <i class="msgtime"><%= message.created_at.strftime("at %l:%M %p") %></i>
  </small>
  <p class="msgcontent"><%= message.content %></p>
 </div>
